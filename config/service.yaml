apiVersion: v1
kind: Service
metadata:
  name: ovnk-mcp-server
  namespace: ovn-kubernetes-mcp
  labels:
    app.kubernetes.io/name: ovn-kubernetes-mcp
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: ovn-kubernetes-mcp
