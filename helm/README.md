# ovnk-mcp-server

![Version: 0.1.0](https://img.shields.io/badge/Version-0.1.0-informational?style=flat-square) ![Type: application](https://img.shields.io/badge/Type-application-informational?style=flat-square) ![AppVersion: latest](https://img.shields.io/badge/AppVersion-latest-informational?style=flat-square)

A Helm chart for the OVN-Kubernetes MCP Server

## Values

| Key | Type | Default | Description |
|-----|------|---------|-------------|
| affinity | object | `{}` | Affinity rules for pod scheduling |
| createNamespace | string | `"auto"` | Whether to create the namespace. `"auto"` = create only if missing, `true` = always create, `false` = never create |
| fullnameOverride | string | `""` | Override the full resource name |
| namespace | string | `"ovnk-mcp"` | Namespace where the MCP server pod and its resources are deployed |
| image.pullPolicy | string | `"IfNotPresent"` | Image pull policy |
| image.repository | string | `"ghcr.io/ovn-kubernetes/ovn-kubernetes-mcp/ovnk-mcp-server"` | Container image repository |
| image.tag | string | `""` | Container image tag (defaults to Chart appVersion) |
| imagePullSecrets | list | `[]` | Secrets for pulling images from private registries |
| mcpServer.debugPodNamespace | string | `"default"` | Namespace where debug pods are created for node debugging (pwru, tcpdump, conntrack, etc.) |
| mcpServer.kernelImage | string | `"nicolaka/netshoot:v0.15"` | Container image for kernel tools (conntrack, ip, iptables, nft) |
| mcpServer.mode | string | `"live-cluster"` | Mode of debugging: live-cluster, offline, or both |
| mcpServer.port | int | `8080` | Port for HTTP transport |
| mcpServer.pwruImage | string | `"docker.io/cilium/pwru:v1.0.10"` | Container image for pwru (packet, where are you?) operations |
| mcpServer.tcpdumpImage | string | `"nicolaka/netshoot:v0.15"` | Container image for tcpdump operations |
| mcpServer.toolTimeout | int | `120` | Timeout in seconds for tool operations (0 to disable) |
| mcpServer.transport | string | `"http"` | Transport protocol: http (recommended for in-cluster) or stdio |
| nameOverride | string | `""` | Override the chart name |
| nodeSelector | object | `{}` | Node selector for pod scheduling |
| podAnnotations | object | `{}` | Additional annotations to add to the pod |
| podLabels | object | `{}` | Additional labels to add to the pod |
| rbac.create | bool | `true` | Whether to create ClusterRole and ClusterRoleBinding |
| replicaCount | int | `1` | Number of replicas for the MCP server deployment |
| resources | object | `{}` | Resource limits and requests for the MCP server container |
| service.port | int | `8080` | Service port |
| service.type | string | `"ClusterIP"` | Service type |
| serviceAccount.annotations | object | `{}` | Annotations to add to the ServiceAccount |
| serviceAccount.create | bool | `true` | Whether to create a ServiceAccount |
| serviceAccount.name | string | `""` | The name of the ServiceAccount to use (generated from fullname if not set) |
| tolerations | list | `[]` | Tolerations for pod scheduling |

----------------------------------------------
Autogenerated from chart metadata using [helm-docs v1.14.2](https://github.com/norwoodj/helm-docs/releases/v1.14.2)
