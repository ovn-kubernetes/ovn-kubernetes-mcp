apiVersion: v1
kind: Service
metadata:
  name: {{ include "ovnk-mcp-server.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "ovnk-mcp-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ovnk-mcp-server.selectorLabels" . | nindent 4 }}
